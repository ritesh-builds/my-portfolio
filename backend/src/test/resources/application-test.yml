spring:
  datasource:
    url: jdbc:h2:mem:portfolio_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false

app:
  jwt-secret: integration-test-secret
  frontend-url: http://localhost:5173
  admin:
    username: admin
    password: Admin@123
